name: "e2e_json_task"
steps:
  - name: "json_step1"
    type: "json"
    count:
      records: 50
    options:
      path: "/tmp/data/json/output"
    fields:
      - name: "user_id"
        type: "string"
      - name: "email"
        type: "string"
      - name: "created_at"
        type: "timestamp"